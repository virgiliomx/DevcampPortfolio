<h1>Portfolio items</h1>

<% @portfolio_item.each do |item| %>
  <p><%= item.title %></p>
  <p><%= item.subtitle %></p>
  <p><%= item.body %></p>
  <%= image_tag item.tumb_image if !item.tumb_image.nil? %>
<% end %>